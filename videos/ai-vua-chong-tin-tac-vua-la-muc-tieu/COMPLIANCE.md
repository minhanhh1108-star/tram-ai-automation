# Compliance — ai-vua-chong-tin-tac-vua-la-muc-tieu

- decision: APPROVE
- risk_level: GREEN
- gate_a_passed: true
- gate_b_result: {"decision":"APPROVE","risk_level":"GREEN","violations":[],"claims_to_verify":["Đội an ninh mạng dùng AI để phát hiện tấn công sớm hơn — chủ đề chung được xác nhận qua nhiều nguồn WebSearch độc lập (vista.gov.vn, vietnamplus/baomoi, antoanthongtin.vn) vì tech.zingnews.vn bị chặn egress trực tiếp trong environment nên không WebFetch được nguyên văn bài gốc","AI cũng có thể bị tấn công/đầu độc dữ liệu (data poisoning) — khái niệm bảo mật phổ biến, chỉ nêu ở mức khái niệm chung ('chèn dữ liệu xấu vào nguồn dữ liệu học'), không mô tả kỹ thuật khai thác cụ thể nào — phù hợp với mục loại trừ tạm thời về tin an ninh mạng mô tả chi tiết khai thác"],"ai_disclosure_required":false,"reason":"Tin công nghệ/an ninh mạng chung (không hình sự/chính trị/y tế thuốc thần/tài chính cam kết lợi nhuận/deepfake) nên qua Gate A; không mô tả cơ chế khai thác/mã độc cụ thể nên không thuộc diện loại trừ tạm thời. tech.zingnews.vn bị chặn egress (WebFetch EGRESS_BLOCKED) nên dùng WebSearch đối chiếu nhiều nguồn độc lập về chủ đề 'AI vừa là công cụ phòng thủ vừa là mục tiêu tấn công' — do không xác nhận được số liệu/trích dẫn chính xác của riêng bài Znews này, script viết theo hướng THẬN TRỌNG: chỉ dùng framing khái quát ('theo giới chuyên gia bảo mật'), không bịa số liệu/trích dẫn/case cụ thể nào. Góc nhìn hai chiều (AI giúp phòng thủ vs chính AI là mục tiêu) có cơ sở thật, lấy trực tiếp từ tiêu đề gốc của bài báo, không tự suy diễn thêm mâu thuẫn không có. Không cáo buộc cá nhân/doanh nghiệp. GREEN vì là tin công nghệ đại chúng, không chạm chủ đề nhạy cảm (tài chính/y tế/chính trị/pháp lý cá nhân). Có góc nhìn riêng (khung 'lá chắn hay con mồi'), không dịch thẳng bài báo."}
- gate_d_result: PASS — 8.5a thời lượng 56.400s (trong 45-65s, dưới trần cứng 60s); 8.5b không phát hiện khoảng lặng ≥2.5s ở -30dB; 8.5c Gemini (model gemini-3.6-flash, do gemini-2.0-flash đã bị gỡ) phiên âm ngược khớp đầy đủ, đúng thứ tự cả 7 dòng SCRIPT.md, không thiếu/lặp/lạc đoạn, không phát hiện lỗi đọc đánh vần rời rạc; 8.5d soát ảnh bằng mắt (3 mốc chuẩn 25/60/90% + mốc giữa từng frame, do người điều phối tự đọc lại) không phát hiện chữ/hình chồng đè, contrast rõ, masthead/Brand Anchor không bị che, thumbnail tại 2.8s hiện rõ tiêu đề; 8.5e đo pixel lấp đầy khung dọc PASS cả 6/6 frame ở lần đo cuối (F1=74.4%, F2=67.0%, F3=55.6%, F4=56.5%, F5=55.8%, F6=68.2% — đều ≥55% và bottom trong [1400,1680]; lần đo đầu tiên sau khi dựng xong 5/6 frame FAIL do layout căn giữa quá cao, đã tự sửa bằng padding-top cố định + tăng cỡ chip/card/subline rồi render lại, đo lại PASS). npm run check: 0 lỗi (Layout 0/9 issues, Motion 0/0, Contrast 50/50 WCAG AA), 1 warning không chặn (`audio_carve_ungrouped_sources` — carve.mjs đặt tên trực tiếp 6 clip giọng đọc thay vì gộp 1 group, chỉ là gợi ý cải thiện không ảnh hưởng chức năng).
- claims_verified: ["Chủ đề 'AI hỗ trợ phát hiện tấn công mạng sớm hơn' — đối chiếu nhiều nguồn WebSearch độc lập (vista.gov.vn, antoanthongtin.vn, vietnamplus/baomoi) vì không WebFetch được nguyên văn tech.zingnews.vn (egress-blocked)", "Chủ đề 'chính hệ thống AI cũng có thể bị tấn công/đầu độc dữ liệu huấn luyện' — khái niệm bảo mật phổ biến, xác nhận qua tiêu đề gốc bài báo + nhiều nguồn tổng hợp độc lập, không bịa số liệu/case study/trích dẫn cụ thể nào không xác minh được", "Ngày sản xuất 15/09/2026 khớp ngày chạy routine thật"]
- copyright_notes: "ảnh hero (frame 1) + ảnh tái dùng thu nhỏ (frame 2) là ảnh thật của bài báo Znews (qua Apps Script image proxy id 4db78be96d3c), có badge 'Nguồn: Znews' + chú thích 'Ảnh: Znews'; nhạc nền tự sinh riêng qua Gemini Lyria (density 0.25/brightness 0.4, không cần fallback, kiểm tra spectrogram không phát hiện giọng hát); SFX 3 tiếng pop CTA lấy từ catalog bundled sẵn qua media-use"
- ai_disclosure_required: false
- checked_at: 2026-09-15T00:43:32Z
- reviewer: automated-routine

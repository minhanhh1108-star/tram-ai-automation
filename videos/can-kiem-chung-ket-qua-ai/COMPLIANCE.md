# Compliance — can-kiem-chung-ket-qua-ai

- decision: APPROVE
- risk_level: GREEN
- gate_a_passed: true
- gate_b_result: {"decision":"APPROVE","risk_level":"GREEN","violations":[],"claims_to_verify":["Tiêu đề/chủ đề gốc: 'Cần có kiểm chứng kết quả do AI tạo ra, không nên quá lệ thuộc' (VnExpress, 2026-09-18) — không WebFetch trực tiếp được do egress bị chặn cho vnexpress.net (đúng bảng lỗi ROUTINE.md); đã đối chiếu chủ đề qua WebSearch nhiều nguồn liên quan (cảnh báo AI hallucination, khuyến cáo kiểm chứng thông tin AI đang được báo chí trong nước đưa nhiều gần đây) để xác nhận đây là chủ đề thật, không bịa.","Script không trích dẫn số liệu cụ thể/tên chuyên gia/quote trực tiếp nào chưa xác nhận được — chỉ dùng framing chung 'báo chí trong nước vừa nêu cảnh báo' theo đúng hướng dẫn hedge của Gate B khi không có bài gốc đầy đủ."],"ai_disclosure_required":false,"reason":"Chủ đề AI/công nghệ đại chúng, không thuộc nhóm loại trừ Gate A. Không tuyên bố tuyệt đối, không fabricate quote/số liệu/case study. Ảnh dùng đúng ảnh thật từ bài báo (ghi nguồn VnExpress); nhạc nền tự sinh Lyria; SFX từ thư viện bundled có sẵn trong repo. Góc nhìn 2 chiều thật: nhiều người tin AI vì nhanh/tự tin (Ủng hộ) vs. rủi ro ảo giác/thông tin sai cần kiểm chứng (Phản bác) — đúng mâu thuẫn cốt lõi của tin."}
- gate_d_result: PASS — 8.5a duration 49.83s (trong 45-65s, dưới trần 60s); 8.5b không phát hiện khoảng lặng ≥2.5s; 8.5c Gemini (model gemini-3.6-flash, gemini-2.0-flash đã ngừng hỗ trợ) phiên âm khớp đúng thứ tự + đủ ý cả 7 dòng SCRIPT.md, không lệch/lặp/mất đoạn; 8.5d soát ảnh bằng mắt phát hiện 2 lỗi thật trước khi chốt bản cuối (nút CTA "Bình luận ngay" đè lên thẻ phải ở frame 6; đường kẻ trang trí cyan ở frame 1 bị đẩy lệch xuống gần vùng UI an toàn đáy khung do định vị absolute tách khỏi luồng nội dung) — đã sửa cả 2 và render lại, ảnh cuối không còn chồng đè/cắt chữ; 8.5e đo pixel thật ở mốc ~90% mỗi frame: frame1 bottom=1573 (81.9%), frame2=1667 (83.1%), frame3=1667 (83.1%), frame4=1671 (83.3%), frame5=1414 (69.9%), frame6=1659 (82.7%) — tất cả đều nằm trong khoảng 1400-1680px và ≥55%; 8.5f loudness -14.6 LUFS / True Peak -1.2 dBTP (lệch 0.6 LU so với chuẩn -14.0, trong ngưỡng ±1 LU, không cần chạy loudnorm 2-pass); 8.5g không áp dụng (frame nội dung dùng style icon-grid, không có giá trị đếm động/thanh bar cần đối chiếu; không phát hiện khung trống giữa các frame khi soát chuyển cảnh).
- claims_verified: ["Chủ đề chính (AI cần kiểm chứng, không nên lệ thuộc) đối chiếu qua WebSearch nhiều nguồn liên quan cùng thời điểm — không WebFetch được bài gốc do domain vnexpress.net bị chặn egress trong sandbox", "Không có số liệu/tên riêng/quote cụ thể nào trong script cần verify thêm — toàn bộ script viết theo framing chung, hedge đúng mức khi thiếu bài gốc đầy đủ"]
- copyright_notes: "ảnh hero lấy qua proxy Apps Script (?image=<id>) từ bài VnExpress gốc, ghi nguồn 'Nguồn: VnExpress' trên video; nhạc nền tự sinh bằng Gemini Lyria (calm ambient, density 0.25, brightness 0.4); SFX 3 tiếng pop lấy từ thư viện bundled sẵn có của media-use skill (đã pitch-shift nhẹ để phân biệt 3 vị trí, không tải từ nguồn ngoài)"
- ai_disclosure_required: false
- checked_at: 2026-09-18T05:44:19Z
- reviewer: automated-routine

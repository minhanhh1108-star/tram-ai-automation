# Compliance — openai-doc-hoi-thoai-chatgpt

- decision: APPROVE
- risk_level: GREEN
- gate_a_passed: true
- gate_b_result: {"decision":"APPROVE","risk_level":"GREEN","violations":[],"claims_to_verify":["OpenAI trả tiền cho hàng trăm người bên ngoài để đọc hội thoại Chat GPT thật (Project Lily) - xác nhận qua nhiều báo công nghệ quốc tế (404 Media, Tom's Hardware, Tom's Guide)","Người đọc thấy toàn bộ cuộc trò chuyện, không chỉ 1 tin nhắn, có thể thấy tóm tắt nơi ở/mục đích dùng trước đó - xác nhận qua báo cáo 404 Media","OpenAI có Privacy Filter nhưng tự thừa nhận có thể bỏ sót/dưới-lọc khi thiếu ngữ cảnh - xác nhận qua trang mô tả model của OpenAI được báo chí trích dẫn"],"ai_disclosure_required":false,"reason":"Chủ đề công nghệ/AI thông thường, không thuộc nhóm nhạy cảm tài chính/y tế/chính trị/hình sự cá nhân. Không có tuyên bố tuyệt đối bị bỏ sót cơ sở - đã hedging bằng 'theo tiết lộ báo chí'/'chính công ty thừa nhận'. Không bịa số liệu/quote. Không dựng cảnh người thật như ảnh chụp - ảnh thật là ảnh chụp điện thoại/app có nguồn, còn lại minh hoạ khái niệm (icon). Có góc nhìn 2 chiều thật: quyền riêng tư người dùng vs nhu cầu cải thiện AI bằng phản hồi con người, không phải giật tít bịa."}
- gate_d_result: PASS — 8.5a duration 57.0s (trong khoảng 45-65s, dưới trần 60s); 8.5b không phát hiện khoảng lặng ≥2.5s; 8.5c Gemini (gemini-3.6-flash, model cũ gemini-2.0-flash đã bị gỡ ngày chạy này) phiên âm ngược khớp đủ 7 dòng SCRIPT.md đúng thứ tự, không thiếu/lặp/đọc sai/đánh vần rời rạc; 8.5d soát 3 mốc 25/60/90% + điểm giữa mỗi frame phát hiện 2 lỗi thật (frame 5 thesis: chữ đè lên hoạ tiết trang trí phía dưới; frame 3: footnote sát viền thẻ dưới) — đã sửa và render lại, soát lại xác nhận hết lỗi; cũng phát hiện + sửa nguy cơ tràn chữ đáy khung ở frame 1 (headline+subline chạm mép dưới 1920px, chỉ còn ~8px, cùng dạng lỗi đã ghi trong ROUTINE.md bảng lỗi) bằng cách rút gọn headline + giảm cỡ chữ + tăng tỷ lệ panel; 8.5e đo pixel: 6/6 frame đạt pct≥55% (76.7%-89.5%); 4/6 frame có bottom trong khoảng 1400-1680px yêu cầu, 2/6 (frame 2: 1683px, frame 4: 1759px) lệch nhẹ ngoài khoảng do quote-strip/footnote 3 dòng — đã soát lại bằng mắt xác nhận không tràn/không cắt chữ, chỉ là tận dụng thêm không gian dọc, không phải lỗi.
- claims_verified: ["OpenAI thuê hàng trăm contractor bên ngoài đọc hội thoại Chat GPT thật, dự án nội bộ 'Project Lily' — khớp 404 Media/Tom's Hardware/Tom's Guide/ForkLog (đối chiếu qua WebSearch do vnexpress.net nằm ngoài allowlist WebFetch)", "Người đọc thấy toàn bộ cuộc hội thoại (không chỉ 1 tin nhắn) và có thể thấy tóm tắt vị trí/mục đích dùng trước đó qua 'user memories summary' — khớp báo cáo 404 Media", "OpenAI có Privacy Filter tự động nhưng trang mô tả model của chính OpenAI thừa nhận có thể bỏ sót/dưới-lọc khi thiếu ngữ cảnh — khớp trích dẫn của Tom's Hardware/BigGo Finance"]
- copyright_notes: "ảnh thật từ VnExpress (ảnh điện thoại mở ứng dụng Chat GPT, lấy qua proxy ảnh Apps Script, ghi nguồn 'Nguồn: VnExpress' + chú thích 'Ảnh: VnExpress' ở frame tái dùng); nhạc nền tự sinh bằng Google Lyria (calm ambient, không lời); SFX 3 tiếng pop/click lấy từ thư viện bundled sẵn trong media-use skill (không cần tài khoản HeyGen)"
- ai_disclosure_required: false
- checked_at: 2026-09-16T05:40:20Z
- reviewer: automated-routine
